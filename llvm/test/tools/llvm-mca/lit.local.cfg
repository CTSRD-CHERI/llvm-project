# Requires a non-empty default triple for these tests
if "default_triple" not in config.available_features:
    config.unsupported = True

if 'llvm-tool-llvm-mca' not in config.available_features:
    config.unsupported = True
